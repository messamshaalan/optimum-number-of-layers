services:
  - type: web
    name: layeroptimum-pro
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app_nicegui.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
